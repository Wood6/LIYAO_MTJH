这几种情况会报段错误：
  1. 链接脚本test.lds中 将S1重定位到一个不可读的地址（譬如 0x1000000），printf("S1 = %d\n", S); 时就段错误了
  2. 链接脚本test.lds中 将S1重定位到一个不可写的地址（譬如 代码段内地址 0x8048500），S1 = 100; 时就段错误了
  3. S2一样
  
只有将S1 和 S2 重定位到可读/可写的地址，在读/写时才不会报错，这样的地址就譬如 .data 段内地址，就是现在的最终代码



